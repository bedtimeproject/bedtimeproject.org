import { render } from "@testing-library/react";
import ChristmasLights from "./ChristmasLights";

describe("renders ChristmasLights", () => {
  test("renders base component", () => {
    render(<ChristmasLights />);
  });
});
