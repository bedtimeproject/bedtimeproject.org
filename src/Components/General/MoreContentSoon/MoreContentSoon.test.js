import { render } from "@testing-library/react";
import MoreContentSoon from "./MoreContentSoon";

describe("renders MoreContentSoon", () => {
  test("renders base component", () => {
    render(<MoreContentSoon />);
  });
});
