import { render } from "@testing-library/react";
import RainyWindow from "./RainyWindow";

describe("renders RainyWindow", () => {
  test("renders base component", () => {
    render(<RainyWindow />);
  });
});
