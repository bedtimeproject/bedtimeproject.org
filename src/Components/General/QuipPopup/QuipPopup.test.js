import { render } from "@testing-library/react";
import QuipPopup from "./QuipPopup";

describe("renders QuipPopup", () => {
  test("renders base component", () => {
    render(<QuipPopup />);
  });
});
