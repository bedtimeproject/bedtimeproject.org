import { render } from "@testing-library/react";
import SecondaryHeadline from "./SecondaryHeadline";

describe("renders SecondaryHeadline", () => {
  test("renders base component", () => {
    render(<SecondaryHeadline />);
  });
});
