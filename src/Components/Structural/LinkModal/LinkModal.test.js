import { render } from "@testing-library/react";
import LinkModal from "./LinkModal";

describe("renders LinkModal", () => {
  test("renders base component", () => {
    render(<LinkModal />);
  });
});
