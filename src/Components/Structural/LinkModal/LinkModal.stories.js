import React from "react";
import LinkModal from "./LinkModal";

export default {
  title: "Structural/LinkModal",
  component: LinkModal,
  argTypes: {},
};

const Template = (args) => <LinkModal {...args} />;

export const Default = Template.bind({});
Default.args = {};
