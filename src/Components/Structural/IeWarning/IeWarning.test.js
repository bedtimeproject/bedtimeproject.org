import { render } from "@testing-library/react";
import IeWarning from "./IeWarning";

describe("renders IeWarning", () => {
  test("renders base component", () => {
    render(<IeWarning />);
  });
});
